openapi: 3.0.3
info:
  title: GrowUp Yourself — Local Persistence Contracts
  version: 1.0.0
  description: OpenAPI-style contracts describing local CRUD operations

components:
  schemas:
    Meta:
      type: object
      required: [id, pilar_id, titulo, duracao_minutos, dias_semana]
      properties:
        id: { type: string }
        pilar_id: { type: string }
        titulo: { type: string }
        descricao: { type: string }
        dias_semana: { type: integer }
        duracao_minutos: { type: integer }
        horario_sugerido: { type: string }
        notificacao_minutos_antes: { type: integer }
        recorrente: { type: boolean }

    Execucao:
      type: object
      required: [id, meta_id, data, status]
      properties:
        id: { type: string }
        meta_id: { type: string }
        data: { type: string, format: date }
        horario_inicio_real: { type: string, format: date-time }
        duracao_real: { type: integer }
        status: { type: string }
        observacao: { type: string }

    UserProfile:
      type: object
      required: [id, nome]
      properties:
        id: { type: string }
        nome: { type: string }
        foto_path: { type: string }
        sexo: { type: string }
        peso: { type: number }
        altura: { type: number }
